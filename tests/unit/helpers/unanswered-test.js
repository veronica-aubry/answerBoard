import { unanswered } from 'answers/helpers/unanswered';
import { module, test } from 'qunit';

module('Unit | Helper | unanswered');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = unanswered([42]);
  assert.ok(result);
});
